test_metadata:
  type: config
  name: acceleration
  description: Create bucket and enable global acceleration


buckets:
  normal-bucket:
    type: default
    acceleration_status: Enabled # Enabled | Suspended
    deletion_policy: 'Retain'
