test_metadata:
  type: config
  name: bucket_origin_access_identity
  description: Create bucket and Origin Access Identity


buckets:
  cloudfront-bucket:
    type: default
    bucket_name: ${EnvironmentName}-cloudfront-bucket
    origin_access_identity: true